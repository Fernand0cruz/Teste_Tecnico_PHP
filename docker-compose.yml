services:
  app:
    build: .
    container_name: teste_tecnico_php
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www
    working_dir: /var/www
    command: sh -c "composer install && php -S 0.0.0.0:8000 -t public"